# SPDX-FileCopyrightText: 2023-2026 Slavi Pantaleev
# SPDX-FileCopyrightText: 2024 IUCCA
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: (Deprecation) Catch and report renamed settings
  ansible.builtin.fail:
    msg: >-
      Your configuration contains a variable, which now has a different name.
      Please change your configuration to rename the variable (`{{ item.old }}` -> `{{ item.new }}`).
  when: "lookup('ansible.builtin.varnames', ('^' + item.old + '$'), wantlist=True) | length > 0"
  with_items:
    - { "old": "tandoor_http_header_content_permission_policy", "new": "tandoor_http_header_permissions_policy" }
    - { "old": "tandoor_container_image_force_pull", "new": "<removed> (the new community.docker.docker_image_pull module handles this natively)" }

- name: (Deprecation) Catch removed Tandoor version 1.x container settings
  ansible.builtin.fail:
    msg: >-
      Your configuration defines `{{ item }}`, which is no longer used since Tandoor switched to one container.
      Remove this setting to use the current defaults, or configure the desired image and networks with
      `tandoor_container_image` and `tandoor_container_additional_networks_custom`.
      Review the values for the current Tandoor version before migrating them; the old API and frontend
      images and network settings cannot be combined automatically.
  when: "lookup('ansible.builtin.varnames', ('^' + item + '$'), wantlist=True) | length > 0"
  loop:
    - tandoor_api_version
    - tandoor_api_container_image
    - tandoor_api_container_image_tag
    - tandoor_api_container_image_registry_prefix
    - tandoor_api_container_image_force_pull
    - tandoor_api_container_additional_networks
    - tandoor_api_container_additional_networks_auto
    - tandoor_api_container_additional_networks_custom
    - tandoor_frontend_version
    - tandoor_frontend_container_image
    - tandoor_frontend_container_image_tag
    - tandoor_frontend_container_image_registry_prefix
    - tandoor_frontend_container_image_force_pull
    - tandoor_frontend_container_additional_networks
    - tandoor_frontend_container_additional_networks_auto
    - tandoor_frontend_container_additional_networks_custom

- name: Fail if required Tandoor settings not defined
  ansible.builtin.fail:
    msg: >-
      You need to define a required configuration setting (`{{ item }}`).
  when: "lookup('vars', item, default='') | string | length == 0"
  with_items:
    - tandoor_uid
    - tandoor_gid
    - tandoor_hostname
    - tandoor_path_prefix
    - tandoor_container_network
    - tandoor_environment_variable_secret

- name: Fail if Tandoor database type invalid
  ansible.builtin.fail:
    msg: >-
      tandoor_database_type must be 'postgres' or 'sqlite'
  when: "tandoor_database_type not in ['postgres', 'sqlite']"

- name: Run if Traefik is enabled
  when: tandoor_container_labels_traefik_enabled | bool
  block:
    - name: Fail if required Tandoor Traefik settings not defined
      ansible.builtin.fail:
        msg: >-
          You need to define a required configuration setting (`{{ item.name }}`).
      when: "item.when | bool and lookup('vars', item.name, default='') | string | length == 0"
      with_items:
        - { "name": "tandoor_container_labels_traefik_hostname", when: true }
        - { "name": "tandoor_container_labels_traefik_path_prefix", when: true }
        - { "name": "tandoor_container_labels_traefik_middleware_basic_auth_users", when: "{{ tandoor_container_labels_traefik_middleware_basic_auth_enabled }}" }

    - name: Fail if tandoor_container_labels_traefik_path_prefix is different than /
      ansible.builtin.fail:
        msg: >-
          tandoor_container_labels_traefik_path_prefix (`{{ tandoor_container_labels_traefik_path_prefix }}`) must be `/`.
          Tandoor does not support hosting under a subpath yet.
      when: "tandoor_container_labels_traefik_path_prefix != '/'"

    # We ensure it doesn't end with a slash, because we handle both (slash and no-slash).
    # Knowing that `tandoor_container_labels_traefik_path_prefix` does not end with a slash
    # ensures we know how to set these routes up without having to do "does it end with a slash" checks elsewhere.
    - name: Fail if tandoor_container_labels_traefik_path_prefix ends with a slash
      ansible.builtin.fail:
        msg: >-
          tandoor_container_labels_traefik_path_prefix (`{{ tandoor_container_labels_traefik_path_prefix }}`) must either be `/` or not end with a slash (e.g. `/tandoor`).
      when: "tandoor_container_labels_traefik_path_prefix != '/' and tandoor_container_labels_traefik_path_prefix[-1] == '/'"
